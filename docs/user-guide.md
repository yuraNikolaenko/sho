# sho — інструкція користувача

## Що це

Десктопна програма для Windows, що швидко знаходить документи, які містять заданий текст. Підходить для пошуку прізвищ, цитат, ідентифікаторів, фрагментів коду тощо в архівах документів.

## Підтримувані формати

| Категорія           | Розширення                                                                         |
|---------------------|------------------------------------------------------------------------------------|
| Звичайний текст     | `.txt .md .csv .tsv .log .xml .json .yaml .yml .ini .cfg .conf .properties .env` |
| Вихідний код        | `.cs .js .ts .py .rb .go .rs .java .kt .c .cpp .h .hpp .sql .sh .ps1 .bat .cmd`  |
| Веб                 | `.html .htm .xhtml .css .scss .less`                                              |
| Документи           | `.pdf .docx .xlsx .pptx .doc .xls .rtf .tex`                                      |

> **Не підтримуються:** скановані PDF (без OCR), архіви, файли під паролем, презентації PowerPoint 97–2003 (`.ppt`).
>
> **Особливості legacy-форматів:**
> - `.xls` — повна підтримка через NPOI (HSSF): значення клітинок, формули (cached-результат), числа, рядки.
> - `.doc` — текст витягується евристикою з OLE-стріму `WordDocument` (UTF-16 LE runs). Збігаються всі реальні слова документа; у тексті можуть зустрічатись поодинокі обривки метаданих, які ігноруються пошуком (вони не псують результати, лише трохи "забруднюють" сніпети).

Кодування detected автоматично: UTF-8 (з/без BOM), UTF-16, CP1251, KOI8-U та інші. Не потрібно вказувати вручну.

## Перший запуск

1. Запусти `Sho.exe` (або `dotnet run --project src\Sho.App`).
2. У верхньому полі **Folder** натисни **Browse…** і обери папку для пошуку. Вкладені папки скануються автоматично.

## Два режими пошуку

Прапорець **Use index** перемикає режим:

- **Use index = ON** (за замовчуванням, **рекомендується**) — пошук іде по заздалегідь побудованому Lucene-індексу. Швидкість мілісекунди, але треба один раз побудувати індекс.
- **Use index = OFF** — кожне натискання **Search** перебирає всі файли в папці. Не потребує підготовки, але повільне для великих папок.

Підказка біля прапорця показує стан індексу: `Index: built` або `Index: not built`.

## Сценарій з індексом

1. Натисни **Build / rebuild index**. Прогрес-бар знизу показує, скільки файлів оброблено.
2. Дочекайся повідомлення `Indexed`.
3. Введи запит у поле **Find**:
   - `Ніколаенко` — знайде всі форми, де ця підстрока зустрічається в індексі.
   - `Ніколаенко Юрій Вікторович` — знайде документи, де ці слова зустрічаються поруч у вказаному порядку (фразовий пошук Lucene).
4. Натисни **Search** (або Enter).
5. Результати з'являються у двох таблицях:
   - **Files (matches)** ліворуч — файли, відсортовані за кількістю збігів.
   - **Lines (context)** праворуч — кожен рядок документа, що містить збіг, із контекстом ±80 символів навколо.
6. **Подвійний клік** на рядку відкриває документ у програмі, призначеній для його розширення (Word для `.docx`, переглядач PDF для `.pdf` тощо).

## Сценарій без індексу (швидкий разовий пошук)

1. Зніми прапорець **Use index**.
2. Введи запит → **Search**. Програма проходить по всіх файлах папки в кілька потоків.
3. Результати такі ж, як у режимі з індексом.

> Якщо текст файлу більший за 64 МБ, читається тільки перші 64 МБ (захист від log-файлів-гігантів).

## Опції пошуку

- **Match case** — увімкнути чутливість до регістру.
- **Whole word** — шукати тільки цілі слова (не як підстроку).

## Синтаксис запиту (тільки для індексного режиму)

Якщо в запиті є хоч один з операторів — `+ - " ( ) * ? ~ ^ : \ [ ] { }` або слова `AND`/`OR`/`NOT` — спрацьовує **Lucene-синтаксис**:

| Запит                                    | Результат                                           |
|-------------------------------------------|----------------------------------------------------|
| `Ніколаенко`                              | substring (як було) ✅                              |
| `Юрій Вікторович`                         | фраза з двох слів у заданому порядку ✅              |
| `"Юрій Вікторович"`                       | те саме, але через QueryParser                     |
| `Юрій AND Вікторович`                     | обидва слова повинні бути в документі (порядок неважливий) |
| `Юрій OR Maria`                           | хоч одне з двох слів                                |
| `Ніколаенко AND NOT тест`                 | `Ніколаенко` є, але `тест` нема                     |
| `+обов'язкове -виключене`                 | `+` обов'язкове, `-` виключити                       |
| `назв*`                                   | префіксний wildcard (`назва`, `назвою`, …)         |
| `назвою~2`                                | fuzzy: до 2 правок (одруки)                         |
| `ext:pdf AND content:Ніколаенко`          | поля: тільки PDF з ім'ям                            |

Brute-force режим завжди шукає буквально (нема Lucene-індексу — нема query parser).

## Тема та мова інтерфейсу

У правому верхньому куті:
- **Іконка місяця/сонця** — перемикач Dark / Light.
- **Випадайка мов** — Українська / English.

Налаштування зберігаються в `%LOCALAPPDATA%\sho\settings.json` і відновлюються при наступному запуску.

## Прогрес індексації

Під час `Build index`/`Search` у нижньому статус-барі видно:
- стадію (`Scanning folder` / `Indexing` / `Committing index`),
- лічильник `1234/5678 (21.7%)`,
- швидкість `4.2 f/s`,
- ETA: `12m34s`,
- ім'я поточного файлу + `(45s on this file)` якщо обробка одного файлу > 2 с.

Якщо bar довго стоїть на одному файлі (велике PDF / DOC) — це нормально; лічильник `on this file` росте кожні 0.5 с, тож ти бачиш, що програма жива.

## Контекстне меню (правий клік на результаті)

- **Open** — відкрити документ.
- **Reveal in Explorer** — показати файл в Провіднику.
- **Copy path** (тільки для файлів) — скопіювати повний шлях.

## Обмеження та FAQ

- **Чому в режимі з індексом не знаходить нове слово?** Індекс — це snapshot. Перебудуй (**Build / rebuild index**) після того, як у папці з'явились нові файли.
- **Де зберігається індекс?** `%LOCALAPPDATA%\sho\indexes\<hash-of-folder>\`. Можеш видалити кнопкою **Delete index** або руками через Провідник.
- **Скільки можу шукати по великій папці?** Перевірено на ~50k документів. Індексація зазвичай 1–10 хв (залежить від розмірів PDF/DOCX). Сам пошук — мілісекунди.
- **Чи можна перервати пошук?** Так, кнопкою **Cancel**.
- **Українська/російська морфологія?** У v1 пошук — за підстрокою без морфології. `Ніколаенко` не знайде `Ніколаенка`. Шукай по корінню або кільком формам окремо.

## Гарячі клавіші

- **Enter** у будь-якому полі → запустити пошук.
- **Escape** → не призначено (зарезервовано).
